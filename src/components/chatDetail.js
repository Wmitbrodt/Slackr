import React from 'react'
import { Alert } from 'react-bootstrap'

export default (props) => {
  return (
    <div>
      <Alert bsStyle="warning">
        <strong>Will</strong> : some random message goes here..
      </Alert>
    </div>
  )
}
